name: Sign IPA and Deploy to GitHub Pages

on:
  workflow_dispatch:
    inputs:
      source_repo:
        description: 'Source repository (owner/repo)'
        required: true
        default: 'pcpapc172/NoveoNative'
      release_tag:
        description: 'Release tag (leave empty for latest)'
        required: false
      force_deploy:
        description: 'Force redeploy (skip version check)'
        required: false
        type: boolean
        default: false
  schedule:
    - cron: '0 */6 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  sign-and-deploy:
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          
      - name: Get latest release info
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          REPO="${{ github.event.inputs.source_repo || 'pcpapc172/NoveoNative' }}"
          echo "Fetching release from: $REPO"
          
          if [ -z "${{ github.event.inputs.release_tag }}" ]; then
            echo "Fetching latest release..."
            RELEASE_DATA=$(curl -s -H "Authorization: token $GH_TOKEN" \
              "https://api.github.com/repos/$REPO/releases/latest")
          else
            echo "Fetching specific release: ${{ github.event.inputs.release_tag }}"
            RELEASE_DATA=$(curl -s -H "Authorization: token $GH_TOKEN" \
              "https://api.github.com/repos/$REPO/releases/tags/${{ github.event.inputs.release_tag }}")
          fi
          
          # Debug: Show release data
          echo "Release data:"
          echo "$RELEASE_DATA" | jq '.'
          
          # Extract info with better error handling
          TAG_NAME=$(echo "$RELEASE_DATA" | jq -r '.tag_name // empty')
          PUBLISHED_AT=$(echo "$RELEASE_DATA" | jq -r '.published_at // empty')
          
          # Find IPA with better error handling
          IPA_URL=$(echo "$RELEASE_DATA" | jq -r '.assets[]? | select(.name | endswith(".ipa")) | .browser_download_url' | head -1)
          
          if [ -z "$TAG_NAME" ] || [ "$TAG_NAME" = "null" ]; then
            echo "❌ Error: Could not find release"
            echo "Response: $RELEASE_DATA"
            exit 1
          fi
          
          if [ -z "$IPA_URL" ] || [ "$IPA_URL" = "null" ]; then
            echo "❌ Error: No IPA found in release $TAG_NAME"
            echo "Available assets:"
            echo "$RELEASE_DATA" | jq -r '.assets[]?.name'
            exit 1
          fi
          
          echo "✅ Found release: $TAG_NAME"
          echo "✅ IPA URL: $IPA_URL"
          
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "published_at=$PUBLISHED_AT" >> $GITHUB_OUTPUT
          echo "ipa_url=$IPA_URL" >> $GITHUB_OUTPUT
          
      - name: Check if already deployed
        id: check
        run: |
          # Force deploy if requested
          if [ "${{ github.event.inputs.force_deploy }}" = "true" ]; then
            echo "already_deployed=false" >> $GITHUB_OUTPUT
            echo "🔄 Force deploy requested, proceeding..."
            exit 0
          fi
          
          if [ -f "version.txt" ]; then
            CURRENT_VERSION=$(cat version.txt)
            if [ "$CURRENT_VERSION" = "${{ steps.release.outputs.tag_name }}" ]; then
              echo "already_deployed=true" >> $GITHUB_OUTPUT
              echo "✅ Version $CURRENT_VERSION already deployed, skipping..."
              exit 0
            fi
          fi
          echo "already_deployed=false" >> $GITHUB_OUTPUT
          echo "🔄 New version detected, proceeding with deployment..."
          
      - name: Download unsigned IPA
        if: steps.check.outputs.already_deployed != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "📥 Downloading IPA from: ${{ steps.release.outputs.ipa_url }}"
          curl -L -H "Authorization: token $GH_TOKEN" \
            -o unsigned.ipa "${{ steps.release.outputs.ipa_url }}"
          
          if [ ! -f unsigned.ipa ]; then
            echo "❌ Failed to download IPA"
            exit 1
          fi
          
          FILE_SIZE=$(stat -f%z unsigned.ipa 2>/dev/null || stat -c%s unsigned.ipa 2>/dev/null)
          echo "✅ Downloaded IPA: $FILE_SIZE bytes"
          ls -lh unsigned.ipa
          
      - name: Setup certificate and provisioning profile
        if: steps.check.outputs.already_deployed != 'true'
        run: |
          echo "🔐 Setting up Enterprise certificate and provisioning profile..."
          
          # Decode certificate
          echo "${{ secrets.P12_CERTIFICATE }}" | base64 --decode > certificate.p12
          
          # Decode provisioning profile
          echo "${{ secrets.PROVISIONING_PROFILE }}" | base64 --decode > profile.mobileprovision
          
          # Create keychain
          security create-keychain -p actions temp.keychain
          security default-keychain -s temp.keychain
          security unlock-keychain -p actions temp.keychain
          security set-keychain-settings -t 3600 -u temp.keychain
          
          # Import certificate
          security import certificate.p12 -k temp.keychain -P "${{ secrets.P12_PASSWORD }}" -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: -s -k actions temp.keychain
          
          # Copy provisioning profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          PROFILE_UUID=$(grep -a -A 1 'UUID' profile.mobileprovision | grep -o '<string>.*</string>' | sed 's/<[^>]*>//g')
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/${PROFILE_UUID}.mobileprovision
          
          echo "✅ Certificate and profile installed"
          echo "📋 Available signing identities:"
          security find-identity -v -p codesigning temp.keychain
          
      - name: Sign IPA (Enterprise)
        id: sign
        if: steps.check.outputs.already_deployed != 'true'
        run: |
          echo "✍️ Signing IPA with Enterprise certificate..."
          
          # Extract unsigned IPA
          mkdir Payload
          unzip -q unsigned.ipa -d Payload
          
          # Find app bundle
          APP_PATH=$(find Payload/Payload -name "*.app" -type d -depth 1 | head -1)
          echo "📦 App path: $APP_PATH"
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ Could not find .app bundle"
            ls -R Payload/
            exit 1
          fi
          
          # Get Enterprise signing identity
          IDENTITY=$(security find-identity -v -p codesigning temp.keychain | grep -E "iPhone Distribution|iOS Distribution" | head -1 | grep -o '"[^"]*"' | tr -d '"')
          
          if [ -z "$IDENTITY" ]; then
            echo "❌ Could not find Enterprise signing identity"
            echo "Available identities:"
            security find-identity -v -p codesigning temp.keychain
            exit 1
          fi
          
          echo "🔑 Signing identity: $IDENTITY"
          
          # Find provisioning profile
          PROFILE_PATH=$(find ~/Library/MobileDevice/Provisioning\ Profiles -name "*.mobileprovision" -type f | head -1)
          echo "📄 Profile: $PROFILE_PATH"
          
          # Extract entitlements from provisioning profile
          security cms -D -i "$PROFILE_PATH" > temp_profile.plist
          /usr/libexec/PlistBuddy -x -c 'Print :Entitlements' temp_profile.plist > entitlements.plist
          
          echo "📋 Entitlements:"
          cat entitlements.plist
          
          # Extract bundle ID from application-identifier in entitlements
          FULL_APP_ID=$(plutil -extract application-identifier raw entitlements.plist 2>/dev/null || echo "")
          
          if [ -n "$FULL_APP_ID" ]; then
            # Remove team ID prefix (everything before first dot)
            BUNDLE_ID=$(echo "$FULL_APP_ID" | sed 's/^[^.]*\.//')
            echo "✅ Extracted Bundle ID from entitlements: $BUNDLE_ID"
          else
            # Fallback to known bundle ID
            BUNDLE_ID="com.etisalat.INDY"
            echo "⚠️ Using fallback Bundle ID: $BUNDLE_ID"
          fi
          
          echo "📱 Bundle ID: $BUNDLE_ID"
          echo "bundle_id=$BUNDLE_ID" >> $GITHUB_OUTPUT
          
          # Remove old signature
          rm -rf "$APP_PATH/_CodeSignature"
          rm -f "$APP_PATH/embedded.mobileprovision"
          
          # Embed new provisioning profile
          cp "$PROFILE_PATH" "$APP_PATH/embedded.mobileprovision"
          
          # Sign all nested code (frameworks, plugins, extensions)
          echo "🔐 Signing nested code..."
          find "$APP_PATH" -d \( -name "*.framework" -o -name "*.appex" -o -name "*.dylib" \) | while read nested; do
            echo "  → $(basename "$nested")"
            /usr/bin/codesign --force --sign "$IDENTITY" \
              --timestamp=none \
              "$nested" 2>&1 || echo "  ⚠️ Warning: Could not sign $nested"
          done
          
          # Sign the main app bundle with entitlements
          echo "🔐 Signing main app bundle..."
          /usr/bin/codesign --force --sign "$IDENTITY" \
            --entitlements entitlements.plist \
            --timestamp=none \
            "$APP_PATH"
          
          # Verify signature
          echo "✅ Verifying signature..."
          codesign -vvv --deep --strict "$APP_PATH"
          
          echo "📋 Signature details:"
          codesign -d --entitlements :- "$APP_PATH" 2>&1 | head -20
          
          # Repackage IPA
          echo "📦 Repackaging IPA..."
          cd Payload
          zip -qry ../NoveoNative.ipa .
          cd ..
          
          # Get file size
          SIZE=$(stat -f%z NoveoNative.ipa 2>/dev/null || stat -c%s NoveoNative.ipa 2>/dev/null)
          SIZE_MB=$(echo "scale=2; $SIZE / 1024 / 1024" | bc)
          echo "✅ Signed IPA: ${SIZE_MB}MB ($SIZE bytes)"
          echo "ipa_size=$SIZE" >> $GITHUB_OUTPUT
          
          # Cleanup
          rm -rf Payload unsigned.ipa certificate.p12 profile.mobileprovision temp_profile.plist entitlements.plist
          security delete-keychain temp.keychain
          
      - name: Update manifest.plist
        if: steps.check.outputs.already_deployed != 'true'
        run: |
          cat > manifest.plist << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>items</key>
              <array>
                  <dict>
                      <key>assets</key>
                      <array>
                          <dict>
                              <key>kind</key>
                              <string>software-package</string>
                              <key>url</key>
                              <string>https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/NoveoNative.ipa</string>
                          </dict>
                      </array>
                      <key>metadata</key>
                      <dict>
                          <key>bundle-identifier</key>
                          <string>${{ steps.sign.outputs.bundle_id }}</string>
                          <key>bundle-version</key>
                          <string>${{ steps.release.outputs.tag_name }}</string>
                          <key>kind</key>
                          <string>software</string>
                          <key>title</key>
                          <string>NoveoNative</string>
                      </dict>
                  </dict>
              </array>
          </dict>
          </plist>
          EOF
          
          echo "📄 Generated manifest.plist:"
          cat manifest.plist
          
      - name: Update apps.json
        if: steps.check.outputs.already_deployed != 'true'
        run: |
          cat > apps.json << EOF
          {
            "name": "NoveoNative Repository",
            "identifier": "com.noveo.repo",
            "sourceURL": "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/apps.json",
            "apps": [
              {
                "name": "NoveoNative",
                "bundleIdentifier": "${{ steps.sign.outputs.bundle_id }}",
                "developerName": "PCPA",
                "subtitle": "Modern Messenger for iOS",
                "version": "${{ steps.release.outputs.tag_name }}",
                "versionDate": "${{ steps.release.outputs.published_at }}",
                "versionDescription": "Latest release with bug fixes and improvements",
                "downloadURL": "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/NoveoNative.ipa",
                "localizedDescription": "A beautiful and modern messaging app with real-time features including typing indicators, seen receipts, file sharing, and more.",
                "iconURL": "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/assets/icon512.png",
                "tintColor": "#3b82f6",
                "size": ${{ steps.sign.outputs.ipa_size || 50000000 }}
              }
            ],
            "news": [
              {
                "title": "NoveoNative ${{ steps.release.outputs.tag_name }} Released!",
                "identifier": "${{ steps.release.outputs.tag_name }}",
                "caption": "Check out the latest features and improvements",
                "date": "${{ steps.release.outputs.published_at }}",
                "tintColor": "#3b82f6",
                "imageURL": "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/assets/icon512.png",
                "notify": true
              }
            ]
          }
          EOF
          
          echo "📄 Generated apps.json"
          
      - name: Save version
        if: steps.check.outputs.already_deployed != 'true'
        run: |
          echo "${{ steps.release.outputs.tag_name }}" > version.txt
          
      - name: Commit and push
        if: steps.check.outputs.already_deployed != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "🚀 Deploy version ${{ steps.release.outputs.tag_name }}"
          git push origin gh-pages
          
      - name: Create deployment summary
        run: |
          echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Version:** ${{ steps.release.outputs.tag_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Published:** ${{ steps.release.outputs.published_at }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Bundle ID:** ${{ steps.sign.outputs.bundle_id }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** ${{ steps.check.outputs.already_deployed == 'true' && '✅ Already deployed' || '✅ Successfully deployed' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📱 Installation Links" >> $GITHUB_STEP_SUMMARY
          echo "- **Web Install:** https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/" >> $GITHUB_STEP_SUMMARY
          echo "- **AltStore:** https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/apps.json" >> $GITHUB_STEP_SUMMARY
          echo "- **Direct IPA:** https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/NoveoNative.ipa" >> $GITHUB_STEP_SUMMARY
